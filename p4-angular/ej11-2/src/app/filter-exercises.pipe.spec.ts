import { FilterExercisesPipe } from './filter-exercises.pipe';

describe('FilterExercisesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExercisesPipe();
    expect(pipe).toBeTruthy();
  });
});
